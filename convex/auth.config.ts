// Clerk auth configuration for Convex
// To enable: set CLERK_ISSUER_URL in the Convex dashboard environment variables,
// then uncomment the provider below.
export default {
  providers: [
    // {
    //   domain: process.env.CLERK_ISSUER_URL,
    //   applicationID: "convex",
    // },
  ],
};
